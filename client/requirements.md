## Packages
zustand | Client-side state management for the shopping cart
framer-motion | Smooth page transitions and hover animations
lucide-react | High-quality icons for the UI

## Notes
- Images use static placeholder URLs from Unsplash (with descriptive comments).
- Zustand is used for persisted client-side cart state.
- Shadcn UI components are assumed to be available in `@/components/ui/`.
